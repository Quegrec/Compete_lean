{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
    <div class="flex flex-col items-center my-5 mx-5 gap-5 pt-48">
        <h1 class="text-3xl font-bold">Connexion</h1>
        {{form_start(loginForm)}}
        <div class="flex flex-col text-center">
            {{form_label(loginForm.name)}}
            {{form_widget(loginForm.name)}}
                
            {{form_label(loginForm.password)}}
            {{form_widget(loginForm.password)}}
            {{form_errors(loginForm)}}
            <button type="submit" class="rounded-full px-6 py-2 my-4 bg-primary text-white">Me connecter</button>
        {{form_end(loginForm)}}
        </div>
        {# <a href="">Pas encore inscrit ?</a>
        <a href="">Mot de passe oublié ?</a> #}
    </div>
{% endblock %}